SUBDIRS =
if ENABLE_mbconvert
  SUBDIRS += converter
endif
if ENABLE_hexmodops
  SUBDIRS += hexmodops
endif
if ENABLE_mbchaco
  SUBDIRS += mbchaco
endif
# if ENABLE_qvdual
#   SUBDIRS += qvdual
# endif
if ENABLE_mbsize
  SUBDIRS += size
endif
if ENABLE_mbskin
  SUBDIRS += skin
endif
if ENABLE_mbtagprop
  SUBDIRS += tagprop
endif
if ENABLE_spheredecomp
  SUBDIRS += spheredecomp
endif
# if ENABLE_vtkMOABReader
#   SUBDIRS += vtkMOABReader
# endif
